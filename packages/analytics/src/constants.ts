// Logger label
export const LOGGER_LABEL = '@mindfiredigital/pivothead-analytics';

// Byte size conversion
export const BYTES_PER_KB = 1024;

// Human-readable byte size unit labels (index matches Math.floor(log(bytes)/log(BYTES_PER_KB)))
export const BYTE_SIZE_LABELS = ['Bytes', 'KB', 'MB', 'GB', 'TB', 'PB'];

// Time conversion constants (in ms / s / m / h)
export const MS_PER_SECOND = 1000;
export const SECONDS_PER_MINUTE = 60;
export const MINUTES_PER_HOUR = 60;
export const HOURS_PER_DAY = 24;
